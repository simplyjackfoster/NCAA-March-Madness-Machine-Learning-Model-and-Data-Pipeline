project:
  target_year: 2026
  base_data_dir: data
  artifacts_dir: artifacts
  outputs_dir: outputs

data:
  random_seed: 42
  num_teams: 64

simulation:
  num_sims: 5000

optimization:
  min_champion_prob: 0.02
  risk_tolerance: balanced
  pool_size: 50
